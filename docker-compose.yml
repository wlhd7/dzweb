services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # 挂载 instance 目录以持久化数据库和上传的文件
      - ./instance:/app/instance
    environment:
      - FLASK_APP=dzweb
      - FLASK_DEBUG=0
      - DZWEB_ADMIN_PASSWORD=admin123
